# Performance

`oxitoken` strives to be the most performant JWT encoding and validation library while simulteneously providing the best developer experience and extensibility. `oxitoken` runs a full suite of benchmarks with each release comparing it against `jsonwebtoken`.

{{#include bench_tables/EncodingPerformance.md}}
{{#include bench_tables/ValidationPerformance.md}}
